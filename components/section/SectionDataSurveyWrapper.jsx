import React from "react";

const SectionDataSurveyWrapper = ({ children }) => {
  return (
    <div className="section-data-survey-wrapper" id="data">
      {children}
    </div>
  );
};

export default SectionDataSurveyWrapper;
